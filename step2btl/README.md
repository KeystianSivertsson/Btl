Step2BTL
=========

Prototype CLI to convert a minimal STEP file into a BTL-like file.

Usage:

```
node ./bin/step2btl.js <input.step> [outputDir]
```

This is a prototype. The STEP parser is a placeholder and must be improved for
real geometry/uttag handling.
